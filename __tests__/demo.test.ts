// import { foo } from '../src/foo';
// import { Trainer, TrainerRow } from '../src/models/Trainer';
// import * as trainerDao from '../src/daos/trainer.dao';
// import * as trainerService from '../src/services/trainer.service'

/*
    Describe is used to describe some feature for testing.
    Describe itself is not a test, but rather a tool for organizing
    related tests together. Note that you can nest describe calls to provide
    further structure to your tests.
*/
/*
describe('getTrainerById', () => {

    test('works with resolves', () => {
        expect.assertions(1);
        return expect(trainerDao.getTrainerById(1)).resolves.toEqual(trainerService.getTrainerById(1));
      });
    });
    // Case 1
    test(`returns 'success 1' when input is 1`, () => {
        expect(getTrainerById(1)).toEqual('success 1');
    });


    // Case 2
     test(`returns 'success 2' when condition is met`, () => {
        expect(feature()).toEqual('intended output');
    });

    // Case 3
     test(`returns 'succes 3' when condition is met`, () => {
        expect(feature()).toEqual('intended output');
    });

    // Case 4
     test(`returns 'something' when condition is met`, () => {
        expect(feature()).toEqual('intended output');
    });

    // Case 5
     test(`returns 'something' when condition is met`, () => {
        expect(feature()).toEqual('intended output');
    });
});
*/
